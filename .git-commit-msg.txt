Add MinimizeStyle "Bubble" (draggable floating icon); extract FloatingBubble shared component; fix RightControl/SetVisible to hide whichever visual form (window, topbar icon, or bubble) is currently active, without altering Minimized state
